import React from 'react'

function Header() {

    return (  
        <div>
            <header>
                <title>Portfolio</title>
            </header>
        </div>
    );
}

export default Header